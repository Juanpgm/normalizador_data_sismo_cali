{
  "//": "Shared build config for all three cron services (deployed via `railway up --service <name>` from this dir). Per-service startCommand and cronSchedule are NOT set here on purpose: they live on each service instance so railway.json cannot override one service with another's command. Apply/inspect them with scripts/railway_setup.py.",
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "restartPolicyType": "NEVER",
    "numReplicas": 1
  }
}
