{
  "//": "cronSchedule here reaches the deployment manifest but NOT the scheduler: Railway reads it from the service instance. Run scripts/railway_setup.py after any change, or the service will build the image and never execute it.",
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python job.py",
    "cronSchedule": "0 * * * *",
    "restartPolicyType": "NEVER",
    "numReplicas": 1
  }
}
